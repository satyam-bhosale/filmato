import { StyleSheet, View } from "react-native";

export default function AuthOptionDivider(){
    return (
        <View>

        </View>
    );
}

const styles = StyleSheet.create({
    container: {
        flex:1,
        display: "flex",
        flexDirection: "row",
        alignItems: "center",
        
    }
})